import classes from "./CreditCard.module.css";

const CreditCard = () => {
  return (
    <div className={classes.card_wrapper}>
      <div className={classes.credit_card}></div>
    </div>
  );
};
export default CreditCard;
