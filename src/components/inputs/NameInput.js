const NameInput = () => {
  return (
    <div>
      <label htmlFor="name-on-card" id="name-on-card"></label>
      <input type="text" placeholder="Name on card" />
    </div>
  );
};

export default NameInput;
